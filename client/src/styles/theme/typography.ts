const fontFamily =
	"'Pretendard Variable', '나눔 고딕', 'NanumGothic', loadNANUM, '맑은 고딕', 'MalgunGothic', sans-serif";

const fontWeight = {
	regular: 400,
	medium: 500,
	bold: 700
};

const fontScale = {
	0: `10px/12px ${fontFamily}`,
	25: `11px/14px ${fontFamily}`,
	50: `12px/16px ${fontFamily}`,
	100: `14px/20px ${fontFamily}`,
	200: `16px/24px ${fontFamily}`,
	300: `18px/26px ${fontFamily}`,
	400: `20px/30px ${fontFamily}`,
	500: `22px/32px ${fontFamily}`,
	600: `24px/36px ${fontFamily}`,
	700: `26px/38px ${fontFamily}`,
	800: `28px/42px ${fontFamily}`,
	900: `30px/46px ${fontFamily}`,
	1000: `32px/48px ${fontFamily}`,
	1100: `34px/50px ${fontFamily}`,
	1200: `36px/52px ${fontFamily}`,
	1300: `38px/54px ${fontFamily}`,
	1400: `40px/56px ${fontFamily}`,
	1500: `52px/70px ${fontFamily}`,
	1600: `70px/80px ${fontFamily}`
};

export const typography = {
	R00: `${fontWeight.regular} ${fontScale[0]}`,
	M00: `${fontWeight.medium} ${fontScale[0]}`,
	B00: `${fontWeight.bold} ${fontScale[0]}`,
	R25: `${fontWeight.regular} ${fontScale[25]}`,
	M25: `${fontWeight.medium} ${fontScale[25]}`,
	B25: `${fontWeight.bold} ${fontScale[25]}`,
	R50: `${fontWeight.regular} ${fontScale[50]}`,
	M50: `${fontWeight.medium} ${fontScale[50]}`,
	B50: `${fontWeight.bold} ${fontScale[50]}`,
	R100: `${fontWeight.regular} ${fontScale[100]}`,
	M100: `${fontWeight.medium} ${fontScale[100]}`,
	B100: `${fontWeight.bold} ${fontScale[100]}`,
	R200: `${fontWeight.regular} ${fontScale[200]}`,
	M200: `${fontWeight.medium} ${fontScale[200]}`,
	B200: `${fontWeight.bold} ${fontScale[200]}`,
	R300: `${fontWeight.regular} ${fontScale[300]}`,
	M300: `${fontWeight.medium} ${fontScale[300]}`,
	B300: `${fontWeight.bold} ${fontScale[300]}`,
	R400: `${fontWeight.regular} ${fontScale[400]}`,
	M400: `${fontWeight.medium} ${fontScale[400]}`,
	B400: `${fontWeight.bold} ${fontScale[400]}`,
	R500: `${fontWeight.regular} ${fontScale[500]}`,
	M500: `${fontWeight.medium} ${fontScale[500]}`,
	B500: `${fontWeight.bold} ${fontScale[500]}`,
	R600: `${fontWeight.regular} ${fontScale[600]}`,
	M600: `${fontWeight.medium} ${fontScale[600]}`,
	B600: `${fontWeight.bold} ${fontScale[600]}`,
	R700: `${fontWeight.regular} ${fontScale[700]}`,
	M700: `${fontWeight.medium} ${fontScale[700]}`,
	B700: `${fontWeight.bold} ${fontScale[700]}`,
	R800: `${fontWeight.regular} ${fontScale[800]}`,
	M800: `${fontWeight.medium} ${fontScale[800]}`,
	B800: `${fontWeight.bold} ${fontScale[800]}`,
	R900: `${fontWeight.regular} ${fontScale[900]}`,
	M900: `${fontWeight.medium} ${fontScale[900]}`,
	B900: `${fontWeight.bold} ${fontScale[900]}`,
	R1000: `${fontWeight.regular} ${fontScale[1000]}`,
	M1000: `${fontWeight.medium} ${fontScale[1000]}`,
	B1000: `${fontWeight.bold} ${fontScale[1000]}`,
	R1100: `${fontWeight.regular} ${fontScale[1100]}`,
	M1100: `${fontWeight.medium} ${fontScale[1100]}`,
	B1100: `${fontWeight.bold} ${fontScale[1100]}`,
	R1200: `${fontWeight.regular} ${fontScale[1200]}`,
	M1200: `${fontWeight.medium} ${fontScale[1200]}`,
	B1200: `${fontWeight.bold} ${fontScale[1200]}`,
	R1300: `${fontWeight.regular} ${fontScale[1300]}`,
	M1300: `${fontWeight.medium} ${fontScale[1300]}`,
	B1300: `${fontWeight.bold} ${fontScale[1300]}`,
	R1400: `${fontWeight.regular} ${fontScale[1400]}`,
	M1400: `${fontWeight.medium} ${fontScale[1400]}`,
	B1400: `${fontWeight.bold} ${fontScale[1400]}`,
	R1500: `${fontWeight.regular} ${fontScale[1500]}`,
	M1500: `${fontWeight.medium} ${fontScale[1500]}`,
	B1500: `${fontWeight.bold} ${fontScale[1500]}`,
	R1600: `${fontWeight.regular} ${fontScale[1600]}`,
	M1600: `${fontWeight.medium} ${fontScale[1600]}`,
	B1600: `${fontWeight.bold} ${fontScale[1600]}`
};
